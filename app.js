const $=s=>document.querySelector(s);let selected=null;
function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[m]))}
async function api(url,opt){const r=await fetch(url,opt);if(!r.ok)throw new Error(await r.text());return r.json()}
async function status(){try{const s=await api("/api/status");$("#dbinfo").textContent="Base: "+Number(s.count).toLocaleString("es-ES")+" vehículos · "+s.dataset+" · CC BY 4.0"}catch(e){$("#dbinfo").textContent="No se pudo consultar la base"}}
function render(list){const box=$("#results");if(!list.length){box.innerHTML='<div class="card"><b>No hay coincidencias.</b><p>Prueba otra búsqueda o usa la búsqueda de evidencia web.</p></div>';return}box.innerHTML=list.map(v=>'<article class="vehicle"><span class="badge">'+esc(v.kind||"vehicle")+'</span><h3>'+esc(v.make)+" "+esc(v.model)+'</h3><p>'+esc(v.years&&v.years!=="[]"?v.years:"Identidad de modelo")+'</p><button class="secondary" data-id="'+esc(v.id)+'">Abrir ficha</button></article>').join("");document.querySelectorAll("[data-id]").forEach(b=>b.onclick=()=>openVehicle(b.dataset.id))}
async function find(){const q=$("#q").value.trim();if(!q){$("#status").textContent="Escribe una marca y modelo.";return}$("#status").textContent="Buscando…";try{const r=await api("/api/vehicles?q="+encodeURIComponent(q));render(r);$("#status").textContent=r.length+" coincidencias"}catch(e){$("#status").textContent="Error: "+e.message}}
async function openVehicle(id){selected=await api("/api/vehicle/"+encodeURIComponent(id));$("#detail").classList.remove("hidden");$("#detail").innerHTML='<div class="eyebrow">FICHA DE IDENTIFICACIÓN</div><h2>'+esc(selected.make)+" "+esc(selected.model)+'</h2><p>Fuente de base: VehiclesDB · ID: '+esc(selected.id)+'</p><div class="grid"><div class="metric"><b>Años</b><span>'+esc(selected.years||"—")+'</span></div><div class="metric"><b>Carrocería</b><span>'+esc(selected.body_types||"—")+'</span></div><div class="metric"><b>Disponibilidad</b><span>'+esc(selected.availability||"—")+'</span></div><div class="metric"><b>Fuente</b><span>VehiclesDB</span></div></div><p>Esta ficha identifica el modelo. Los pares, fluidos, distribución, diagnosis y reparaciones requieren evidencia técnica específica.</p><button id="research" class="secondary">Investigar fuentes técnicas</button>';$("#research").onclick=research;$("#detail").scrollIntoView({behavior:"smooth"})}
async function research(){const q=$("#q").value.trim();if(!selected&&!q)return;const base=selected||{};$("#webresults").classList.remove("hidden");$("#webresults").innerHTML="<b>Buscando evidencia pública…</b>";try{const r=await api("/api/research",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({vehicle_id:base.id||null,make:base.make||q,model:base.model||"",category:"technical specifications"})});if(!r.ok)throw new Error(r.error);$("#webresults").innerHTML='<div class="eyebrow">EVIDENCIA WEB</div><h3>'+esc(r.query)+"</h3>"+r.results.map(x=>'<div class="evidence"><a class="source" target="_blank" rel="noopener" href="'+esc(x.url)+'">'+esc(x.title||x.url)+'</a><div class="small">'+esc(x.source_class)+' · '+esc(x.confidence)+'</div><p class="small">'+esc(x.snippet||"Sin extracto")+"</p></div>").join("")}catch(e){$("#webresults").innerHTML="<b>No se pudo consultar Internet.</b><p>"+esc(e.message)+"</p>"}}
$("#find").onclick=find;$("#internet").onclick=research;$("#q").addEventListener("keydown",e=>{if(e.key==="Enter")find()});$("#refresh").onclick=async()=>{if(!confirm("Actualizar la base abierta?"))return;$("#status").textContent="Actualizando…";try{const r=await api("/api/database/refresh",{method:"POST"});$("#status").textContent=r.ok?"Base actualizada: "+Number(r.count).toLocaleString("es-ES")+" vehículos":"No se pudo actualizar: "+r.error;await status();await find()}catch(e){$("#status").textContent="Error: "+e.message}};status();